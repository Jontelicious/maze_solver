# maze_solver

## Virtual environment

Create a project virtual environment and activate it (Linux/macOS):

```bash
python3 -m venv .venv
source .venv/bin/activate
```

Upgrade pip inside the venv and install requirements:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

To deactivate:

```bash
deactivate
```
